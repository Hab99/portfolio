---
import { getContent } from "@/lib/getContent";
import { site } from "@/data/site";

const { class: className = "", ...rest } = Astro.props;
---

<span class={className} data-editable="nav.logo" {...rest}>
  {getContent("nav.logo", site.nomeCurto)}
</span>
